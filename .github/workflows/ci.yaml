name: CI
on:
  push: null
jobs:
  action:
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: ./
      - run: ormolu --version
    strategy:
      matrix:
        os:
          - macos-latest
          - ubuntu-latest
          - windows-latest
